client localhost {
        ipaddr = 127.0.0.1
        proto = *
        secret = testing123
        nas_type = other
}

client freeradius{
        ipaddr = 10.7.96.17
        proto = *
        secret = testing123
        nas_type = other
#        limit {
#                max_connections = 16
#                lifetime = 0
#                idle_timeout = 30
#        }
}

# IPv6 Client
client localhost_ipv6 {
        ipv6addr        = ::1
        secret          = test123
}
